- if object.try(:notes) && object.notes.any?
  .Notes
    - if title
      h2 Notes

    .Notes-List
      - object.notes.not_referencing_note.find_each do |note|
        = render '/rails_admin/custom/note', note: note
